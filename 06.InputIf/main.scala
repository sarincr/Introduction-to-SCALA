object Main {
  def main(args: Array[String]): Unit = {
        println ("Enter Age : ") 
        var age:Int = scala.io.StdIn.readInt() 
        if(age > 18){  
        println ("Age is greate than 18")  
    }  
  }
}
